
// This one was way too much to write
// k = level #
// process follows at level 0 values are 1 to 1 ratio [1,1][2,2]
// at level 2 = [1, 10^2^k-1], [10^2^k-1 + 1, 2 * 10^2^k-1], [i * 10^2^k-1 + 1, (1 + i) * 10^2^k-1]
function processData(input) {
    //Enter your code here
    
} 